package com.cuupa.classificator.services.kb.semantic.dataExtraction

object Months {
    fun get(value: String): String {
        val month = value.trim()
        when {

        }

        return month
    }
}
